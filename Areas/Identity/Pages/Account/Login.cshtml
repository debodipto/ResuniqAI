@page
@model LoginModel
@{
    ViewData["Title"] = "Login";
}

<style>
    body {
        background: #0b1220;
        font-family: 'Segoe UI', sans-serif;
    }

    .auth-box {
        max-width: 420px;
        margin: 80px auto;
        background: #111827;
        padding: 30px;
        border-radius: 18px;
        box-shadow: 0 15px 40px rgba(0,0,0,0.5);
        color: white;
    }

    .auth-title {
        text-align: center;
        margin-bottom: 20px;
        font-weight: 700;
        font-size: 26px;
    }

    .form-control {
        background: #1f2937;
        border: none;
        color: white;
        border-radius: 10px;
    }

    .form-control:focus {
        background: #1f2937;
        color: white;
        box-shadow: none;
        border: 1px solid #6366f1;
    }

    .btn-main {
        width: 100%;
        border-radius: 10px;
        padding: 10px;
        font-weight: 600;
    }

    a {
        color: #60a5fa;
        text-decoration: none;
    }

    a:hover {
        text-decoration: underline;
    }
</style>

<div class="auth-box">

    <div class="auth-title">Login to Resuniq AI</div>

    <form method="post">

        <div class="mb-3">
            <label>Email</label>
            <input asp-for="Input.Email" class="form-control" />
        </div>

        <div class="mb-3">
            <label>Password</label>
            <input asp-for="Input.Password" type="password" class="form-control" />
        </div>

        <button type="submit" class="btn btn-warning btn-main">
            Login
        </button>

        <div class="mt-3 text-center">
            <a asp-page="./Register">Create new account</a>
        </div>

    </form>

</div>
